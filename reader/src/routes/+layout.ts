// Fully static app: every page is prerendered at build time and served from
// GitHub Pages, so directory-style URLs need the trailing slash.
export const prerender = true;
export const trailingSlash = 'always';
